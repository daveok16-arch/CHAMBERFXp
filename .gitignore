node_modules/
build/
# dist/ is committed so `npm run start` works even when a
# platform (e.g. Render) skips the build step. Rebuilds overwrite it.
# dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
__pycache__/
*.pyc
trading_bot.db
data/
engine/models/model_*
engine/models/latest
engine/models/latest_metadata.json
*.db-shm
*.db-wal
